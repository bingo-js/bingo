---
"bingo-testers": minor
---

`diffCreatedDirectory` now returns file diffs in the colored, line-by-line format test runners print for failed string comparisons, highlighting the changed segments within lines.
`processText` may now be passed as an optional `processText` property of an options object instead of as a required function.
